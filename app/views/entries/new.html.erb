<h1>User: <%= current_user.name %> </h1>
<h2>New Entry Page</h2>



<%= form_for [@user, @entry] do |f| %>
<%= f.label :Change_to_Custom_Date_if_not_for_today%><br><br>
<%= f.date_select :custdate %><br><br>
<%= f.label :title %>
<%= f.text_field :title%><br>
<%= f.label :post %><br>
<%= f.text_area :post, class: "post" %>

<%= f.submit "Save" %>
<% end %>

<div class = "outputArea">
<div id = "titleSpace"><b></b>
<br><br></div>

<div id = "postSpace">
</div>
</div>


<%# <script>
let titleArea = document.querySelector("#titleSpace > b")

let getTitle = document.querySelector("#entry_title")
getTitle.addEventListener("keydown", function(e) {
  //  if (getTitle.value.length > 5) debugger
    if (e.keyCode==8) {
        titleArea.innerText = titleArea.innerText.slice(0, -1)
    }
    titleArea.innerText = e.target.value
})

let postArea = document.querySelector("#entry_post")

let postOutput = document.querySelector("#postSpace")
postArea.addEventListener("keydown", function(e) {
    console.log(e.keyCode)
    if (e.code=="Enter") { 
        e.preventDefault()
        postOutput.innerHTML += `<br>`
        } else if (e.keyCode==8) {
            console.log("back")
           postOutput.innerText = postOutput.innerText.slice(0, -1)
        } else if (e.keyCode==32) {
            console.log("space")
            postOutput.innerText += ' '
        } else 
        {
            console.log("normal")
            postOutput.innerText += e.target.value.slice(-1)
        }
})


</script> %>